% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_climate.R
\name{link_climate}
\alias{link_climate}
\title{Link biological observations to climate exposure windows}
\usage{
link_climate(
  data,
  lat_col = "latitude",
  lon_col = "longitude",
  date_col = "date",
  id_col = NULL,
  variables = "temperature",
  windows = c(1, 7, 30),
  lag = 0,
  buffer_km = 0,
  aggregations = NULL,
  include_same_day = TRUE,
  add_coverage = TRUE,
  coord_decimals = 3,
  project_id = NULL,
  progress = TRUE
)
}
\arguments{
\item{data}{A data.frame of observations.}

\item{lat_col, lon_col}{Names of the latitude / longitude columns (decimal
degrees, WGS84).}

\item{date_col}{Name of the observation-date column. Dates, POSIXct or
"YYYY-MM-DD" character are all accepted.}

\item{id_col}{Optional identifier column name (carried through untouched).}

\item{variables}{Character vector of climate variables to link. One or more of
`"temperature"`, `"precipitation"`, `"humidity"`, `"wind"`, `"solar"`,
`"pressure"`, `"evapotranspiration"`.}

\item{windows}{Integer vector of window lengths in days. A window of `N` covers
the `N` days ending on (and including) the observation date.}

\item{lag}{Days to shift every window back before the observation date
(e.g. `lag = 1` ends each window the day before the event).}

\item{buffer_km}{Spatial buffer radius in km. `0` samples the exact pixel;
a positive value averages over land pixels within the buffer, which is the
robust choice for coastal or water-adjacent sites where the exact pixel may
be masked (returns `NA`).}

\item{aggregations}{Optional named list overriding the default aggregation(s)
per raw column, e.g. `list(tmean_celsius = c("mean", "max"))`.}

\item{include_same_day}{If `TRUE`, also return the raw same-day value of each
variable.}

\item{add_coverage}{If `TRUE`, add data-coverage columns.}

\item{coord_decimals}{Decimals to round coordinates to when grouping sites
(3 is roughly 100 m).}

\item{project_id}{Google Earth Engine / Cloud project id to initialise with.}

\item{progress}{If `TRUE`, print per-site progress.}
}
\value{
A data.frame: `data` with the climate columns appended (original row
  order preserved).
}
\description{
For each row of `data` (which must carry a latitude, a longitude and a date),
attach climate aggregates over the days leading up to the observation. Earth
Engine is queried once per unique site, then windows are computed locally, so
repeated field sites are cheap.
}
\details{
Output columns are named `{variable}_{aggregation}_{N}d` — for example
`tmean_celsius_mean_30d` is the mean daily mean-temperature over the 30 days
ending on the observation date. Same-day raw values are named `{variable}`
(e.g. `tmean_celsius`). A `coverage_{N}d` column reports the fraction of each
window's days that had data (watch for values < 1, which flag gaps or masked
pixels).
}
\examples{
\dontrun{
obs <- data.frame(
  id = c("a", "b"),
  latitude = c(-26.2678, -33.9249),
  longitude = c(27.8607, 18.4241),
  date = as.Date(c("2021-01-15", "2021-03-10"))
)
linked <- link_climate(
  obs,
  variables = c("temperature", "precipitation"),
  windows = c(1, 7, 30),
  buffer_km = 10,          # robust for the coastal Cape Town point
  project_id = "your-gee-project"
)
}
}
